version: "3.8"

services:
  webdav:
    build:
      context: .
      dockerfile: docker/Dockerfile.webdav
    ports:
      - "8080:8080"
    volumes:
      - webdav-data:/srv/webdav
    restart: unless-stopped

volumes:
  webdav-data:
