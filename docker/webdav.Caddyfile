# Local WebDAV server for testing
:8080 {
    # Basic WebDAV setup
    route /webdav/* {
        root * /srv/webdav
        webdav
    }

    # Health check endpoint
    route /health {
        respond "OK" 200
    }

    # Log to stdout
    log {
        output stdout
        format console
    }
}
